// src/types/index.ts

export type TResumeItem = {
  title: string;
  institution: string;
  date: string;
  description: string;
};
export type TExperience = {
  title: string;
  companyName: string;
  date: string;
  icon: string;
  iconBg: string;
  points: string[];
};
